memory_type: trigger
directory: triggers
description: Conditions that tend to precede a need, action, or intervention.
operation_mode: evidence_then_aggregate
merge_op: patch
route_group: relevant
overview_title: Scene Triggers
content_template: |
  {{ content }}
embedding_template: |
  trigger
  {{ title }}
  {{ tags }}
  {{ abstract }}
  {{ content }}
overview_template: |
  # {{ overview_title }}

  - memory_count: {{ memory_count }}
  - evidence_count: {{ evidence_count }}
  - top_tags: {{ top_tags }}

  ## Strongest memories
  {{ strongest_memories }}
