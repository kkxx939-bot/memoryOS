memory_type: habit
directory: habits
description: Repeated behavior patterns that can support prediction.
operation_mode: evidence_then_aggregate
merge_op: patch
route_group: relevant
overview_title: Behavior Habits
content_template: |
  {{ content }}
embedding_template: |
  habit
  {{ title }}
  {{ tags }}
  {{ abstract }}
  {{ content }}
overview_template: |
  # {{ overview_title }}

  - memory_count: {{ memory_count }}
  - evidence_count: {{ evidence_count }}
  - top_tags: {{ top_tags }}

  ## Strongest memories
  {{ strongest_memories }}
