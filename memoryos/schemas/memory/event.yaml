memory_type: event
directory: events
description: Timestamped or episodic facts that should remain auditable.
operation_mode: add_only
merge_op: replace
route_group: recent
overview_title: Auditable Events
content_template: |
  {{ content }}
embedding_template: |
  event
  {{ title }}
  {{ tags }}
  {{ abstract }}
  {{ content }}
overview_template: |
  # {{ overview_title }}

  - memory_count: {{ memory_count }}
  - evidence_count: {{ evidence_count }}
  - top_tags: {{ top_tags }}

  ## Strongest memories
  {{ strongest_memories }}
