memory_type: preference
directory: preferences
description: Explicit likes, dislikes, defaults, and response preferences.
operation_mode: upsert
merge_op: patch
route_group: stable
overview_title: Explicit Preferences
content_template: |
  {{ content }}
embedding_template: |
  preference
  {{ title }}
  {{ tags }}
  {{ abstract }}
  {{ content }}
overview_template: |
  # {{ overview_title }}

  - memory_count: {{ memory_count }}
  - evidence_count: {{ evidence_count }}
  - top_tags: {{ top_tags }}

  ## Strongest memories
  {{ strongest_memories }}
